version: '3.8'

services:
  web:
    build: .
    restart: always
    ports:
      - "6010:6010"
    volumes:
      - .:/app
      - ./instance:/app/instance  # Maps your local folder to the container
    env_file:
      - .env